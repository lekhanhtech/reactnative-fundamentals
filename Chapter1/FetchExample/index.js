/**
 * @format
 */

import {AppRegistry} from 'react-native';
import FetchExample from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FetchExample);
